import { dataAccessFirebase } from './data-access-firebase';

describe('dataAccessFirebase', () => {
  it('should work', () => {
    expect(dataAccessFirebase()).toEqual('data-access-firebase');
  });
});
